# import Todo from "Todo.graphql"
# import TodoInput from "Todo.graphql"

type Query {
	todo(id: ID!): Todo
	todos: [Todo]
}

type Mutation {
	add(input: TodoInput!): Todo
	delete(input: TodoInput!): Todo
}
