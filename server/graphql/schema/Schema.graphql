# import Exercise from "Exercise.graphql"
# import ExerciseInput from "Exercise.graphql"

type Query {
	exercise(id: ID!): Exercise
	exercises(input: ExerciseInput): [Exercise]
}

type Mutation {
	add(input: ExerciseInput!): Exercise
	delete(input: ExerciseInput!): Exercise
}
